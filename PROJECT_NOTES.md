# SYMC 마케터 활동일지 — 프로젝트 인수인계 노트

세종청소년음악센터(소리지음) 마케터 홍보단 5~6명이 쓰는 활동일지 웹앱.
학생은 활동을 기록하고, 담당 선생님은 전체를 열람해 관리하며, 최종적으로
학생 개인 포트폴리오로도 쓰는 것이 목표.

## 저장소 / 배포 구조

- **프론트엔드**: `index.html` 하나. GitHub Pages로 배포됨
  (`https://s1nwoo.github.io/sync-activity-log/` 형태의 무료 계정 공개 저장소).
  → 무료 GitHub Pages는 **비공개 저장소로 게시 불가**. 저장소를 private으로
    바꾸면 사이트가 죽는다. 반대로 public 저장소라도 배포된 사이트의 HTML/JS는
    "페이지 소스 보기"로 누구나 열람 가능하다는 점을 항상 전제할 것
    (그래서 비밀값을 절대 프론트 코드에 하드코딩하지 않는다).
- **백엔드**: `Code.gs` — Google Apps Script. 별도의 구글 스프레드시트에 붙여서
  "웹 앱"으로 배포(`실행 자격: 나`, `액세스 권한: 모든 사용자`). 배포 후 나온
  `https://script.google.com/macros/s/.../exec` 주소를 `index.html` 상단의
  `const API_ENDPOINT = "..."`에 넣어야 서버 연동이 살아난다.
  **코드를 고칠 때마다 "새 배포"가 아니라 "배포 관리 → 수정 → 새 버전"으로
  재배포해야 반영된다.**
- **데이터**: 스프레드시트 안에 두 개 시트.
  - `activities` — 실제 활동 기록. 컬럼: id, member, kind, date, start, end,
    place, types(JSON), offline, etc, topic, duty, make(JSON), up(JSON),
    part(JSON), content, evaluation, next, savedAt
  - `members` — 로그인 계정부. 컬럼: 이름 / 코드 / 역할(admin 또는 student).
    코드 열은 텍스트 서식(`@`)으로 강제해둠(0으로 시작하는 코드 보호).

## 로그인 / 권한 구조

- 로그인은 "이름 + 본인 코드"만 쓰는 가벼운 방식(정식 회원가입 아님).
  서버(`Code.gs`)가 `members` 시트와 대조해서만 통과시킨다. **클라이언트
  단독으로는 절대 인증을 통과시키지 않도록 되어 있음** — 이전에 API_ENDPOINT가
  비어 있을 때 아무 이름이나 로그인되던 버그를 고친 것이므로, 이 원칙을
  다시 깨지 않도록 주의.
- `role: student` → 본인이 쓴 기록만 조회/수정/삭제 가능. 서버가
  `entriesFor()`에서 필터링해서 내려주므로, 학생 화면 자바스크립트에는애초에
  다른 학생 데이터가 전송되지 않음(브라우저 개발자도구로 봐도 안 보임).
- `role: admin` → 전체 학생 기록 열람 가능. 프론트에서 `.adminOnly` 클래스가
  붙은 필터(작성자 선택 등)만 admin에게 노출.
- 로그인 세션은 `localStorage`에 저장하고 유효기간 30일
  (`SESSION_DAYS` 상수, index.html 상단). 접속할 때마다 갱신됨. 새로고침 시
  세션이 있으면 로그인 화면을 건너뛰고 바로 진입. `members` 시트에서 코드를
  바꾸거나 삭제하면 다음 접속 때 자동으로 튕겨나가 재로그인 요구.

## 입력 폼 핵심 규칙 (요구사항에서 확정된 것들)

1. 콘텐츠 **제작**과 **업로드**는 서로 다른 카운트 — 절대 합산해서 하나의
   수치로 보여주지 않는다. 통계 화면에도 "제작 합계"와 "업로드 합계"를
   따로 표기하고, "제작 대비 업로드율" 표를 추가로 둠.
2. 활동 시간은 30분 단위 드롭다운(07:00~23:00)만 허용. 자유 입력(분 단위) 금지.
3. 활동장소는 드롭다운 3종 고정: "6F 회의실(청운위실 포함)", "5F 라이브러리",
   "5F 기획단실" + "기타(직접입력)".
4. 기본정보·활동유형·활동데이터 등 표시된 모든 필드는 필수. 하나라도
   비어있으면 저장 자체가 막히고, 문제 있는 칸으로 스크롤 이동 + 빨간 테두리
   + 토스트 안내가 뜬다.
5. "온라인 콘텐츠 기획/제작" 또는 "오프라인 홍보"를 체크했을 때만 활동데이터
   카드가 나타난다(온라인 체크 시 제작·업로드 입력창, 오프라인 체크 시
   참여실적 입력창). 그 외(정기회의만 체크 등)에는 활동데이터 자체가 숨겨짐.
   → 이 규칙 때문에 원래 있던 "회의용/활동용" 수동 토글은 제거함.

## 완료된 것

- 기본 CRUD(작성/수정/삭제), 필터, CSV 내려받기
- 통계 탭: 월별 추이, 플랫폼별 제작/업로드 분리 차트, 활동유형 분포,
  참여 대상, (admin 한정) 멤버별 활동량
- 포트폴리오 탭: 기간/작성자로 필터한 활동을 시간순 문서로 정리, 인쇄(PDF 저장)
- 로그인 게이트, 세션 유지, 성공/실패 애니메이션(로그인 실패 시 흔들림,
  저장 성공 시 체크마크 오버레이, 토스트 색상 구분)
- 소리지음 로고를 SVG symbol로 파일 내부에 심어 상단바/로그인 화면에 배치
  (`logo.svg` 원본을 `<symbol id="symcLogo">`로 감싸 인라인 삽입, 외부 이미지
  파일 의존 없음)

## 다음 단계 — 포트폴리오 화면 개선 (아내 피드백, 아직 미착수)

요청 배경: 현재 포트폴리오 화면이 "활동보고서"에 가깝고, 개인의 대표 활동·
결과물이 시각적으로 드러나지 않음. 원하는 최종 구조:

**개인 프로필 → 누적 성과 → 대표 프로젝트 → 주요 역량 → 전체 활동 기록**

단계별로 난이도를 나눠서 이렇게 진행하기로 함:

### 1단계 (데이터 구조 변경 없이 가능 — 우선 착수)
- 상단 요약을 "이름 | 소속 · 활동기간 · 활동유형 해시태그" 형태의 프로필
  헤더로 재구성 (기존 KPI 숫자는 유지)
- 활동유형(체크박스 6종) 발생 횟수를 세어 "#촬영편집 6회" 식의 태그로 노출
  가능 — 단, "기획력", "협업" 같은 세밀한 역량 분류는 지금 데이터로는 불가
  (활동유형 6종 그대로만 집계 가능)
- 전체 활동 기록 카드를 기본은 요약(날짜·주제·유형·핵심 한 줄)만 보이고
  클릭하면 상세(활동내용/평가/다음계획)가 펼쳐지는 접이식 구조로 변경

### 2단계 (시트 컬럼 추가 필요, 중간 난이도)
- 일지 작성 시 "대표 활동으로 표시" 체크 + "한 줄 성과"(예: 릴스 1건 /
  참여자 120명) 입력칸 추가 → `activities` 시트에 컬럼 2개 추가,
  `Code.gs`의 `HEADERS`/`rowToEntry`/`entryToRow`도 함께 수정 필요
- 포트폴리오 상단에 대표 활동만 뽑아 카드로 크게 노출
- 역량을 태그로 받고 싶다면 일지 작성 화면에 별도 체크박스(기획/제작/
  촬영편집/SNS운영/현장운영/협업 등)를 새로 만들어야 함 — 학생 입력 부담이
  늘어나는 트레이드오프 있음, 실제 사용자(학생) 의견 확인 후 결정 권장

### 3단계 (보류 — 정책 확인 먼저 필요)
- 결과물 이미지(포스터, 부스 사진, 기획안 등) 첨부는 원하는 효과가 가장 크지만
  구현 난이도도 가장 높음:
  - 구글 시트 셀에는 이미지를 못 넣음 → 구글 드라이브에 업로드 후 시트에는
    파일 링크만 저장하는 구조 필요. `Code.gs`에 업로드용 엔드포인트 추가 작업.
  - 휴대폰 사진 용량이 커서 업로드 전 브라우저 단에서 리사이즈 처리 필요
  - 드라이브에서 이미지를 웹에 노출하려면 "링크가 있는 사람은 볼 수 있음"으로
    공개해야 함 → **청소년이 나온 사진이라면 이건 기술이 아니라 기관 정책
    문제. 센터의 초상권/개인정보 규정을 먼저 선생님과 확인해야 함.**
  - 대안: 사진을 직접 올리는 대신 이미 게시된 인스타/유튜브 **링크만** 받아
    카드로 보여주는 방식 — 업로드 기능도, 드라이브 용량도, 공개 설정 고민도
    필요 없어 훨씬 저렴함. 대신 썸네일 자동 추출은 안 되고 SNS에 없는
    자료(회의사진, 기획안 등)는 못 담음.
  - "조회수 2,300" 같은 SNS 지표는 자동 연동 불가 — 학생이 직접 입력해야 함.
  - "배운 점" 텍스트를 AI가 읽어 역량으로 자동 분류하는 것은 **현재 구조로
    불가능**. AI API 키를 프론트에 넣으면 소스보기로 그대로 노출되기 때문
    (코드 하드코딩 위험과 동일한 문제). 하려면 별도 백엔드가 있어야 함.

## 주의해서 지켜야 할 원칙

- 절대 비밀값(로그인 코드, API 키 등)을 `index.html`(프론트) 안에 평문으로
  넣지 않는다 — GitHub Pages는 공개 정적 파일이라 누구나 소스를 본다.
  인증·비밀 관련 로직은 반드시 `Code.gs`(서버) 쪽에서 처리.
- `Code.gs`를 수정하면 반드시 "새 버전으로 재배포"해야 실제로 반영됨
  (수정만 저장하고 재배포를 잊는 실수가 나기 쉬움).
- 학생 화면과 관리자 화면의 데이터 노출 범위는 서버 필터링으로 강제되어야
  하며, 프론트엔드의 화면 숨김(`hidden` 처리)만으로 막았다고 착각하지 않는다.
